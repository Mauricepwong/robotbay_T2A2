

<div class="position-relative jumbotron overflow-hidden p-3 p-md-5 m-md-3 text-center bg-light">
    <div class="col-md-5 p-lg-5 mx-auto my-5">
        <h1 class="display-4 font-weight-normal">RobotBay</h1>
            <p class="lead font-weight-normal">The new marketplace for all your robot needs.</p>
            <%= link_to "Avaliable Robots", robots_path, class: "btn bg-dark btn-lg text-light"%>
                <% if user_signed_in? %>
                <% else %>
                    <%= link_to "Sign up", new_user_registration_path, class: "btn bg-dark btn-lg text-light"%>
                <% end %>
    </div>
</div>