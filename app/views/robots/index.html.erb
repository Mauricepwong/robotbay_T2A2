<h1>Robot Listings </h1>

<table>
 <tr>
  <strong>Sort by: </strong><br />
   <th><%= link_to "Robot Name", :sort => "name"%> </th>
   <th><%= link_to "Price", :sort => "price"%> </th>
 </tr>
 </table>

<div class = "container" >
  <table class= "table" >
    <tr>  
      <th>Robot Name</th>
      <th>Price</th>
      <th>Image</th>
      <th>Seller</td>
    </tr>
    <% @robots.each do |robot| %>
    <tr>
        <td><%= link_to robot.name, robot_path(robot[:id]) %></td>
        <td>$<%= robot.price %></td>
        <td class="image" ><%= image_tag(robot.image) if robot.image.attached? %>  </td>
        <td><%= robot.user.username %></td>
    </tr>
    <% end %>  
  </table>
</div>